To set up the backend and database, ensure the following prerequisites are met:

Database: PostgreSQL
Java Development Kit: Java SDK 17
Build Tool: Gradle (Groovy DSL)
The database credentials can be found in the application.properties file located within the project directory.


To run the frontend, you’ll need the following:

Node.js: Version 22.12.0
Angular CLI: Version 19.0.5
For styling, install Bootstrap using the command:

bash
npm install bootstrap
Once installed, import Bootstrap into your project by adding its path to the styles section of the angular.json file.